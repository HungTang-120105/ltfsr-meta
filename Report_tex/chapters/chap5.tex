\chapter{Huấn luyện và đánh giá}
\label{chap:results}

Chương này trình bày quy trình huấn luyện thực tế, kết quả định lượng trên cả hai bộ dữ liệu, các
nghiên cứu loại trừ (ablation), phân tích định tính và phần thảo luận. Mọi con số đều được lấy trực
tiếp từ các tệp kết quả trong \texttt{outputs/cifar/*.csv} và \texttt{outputs/cub\_200\_2011/*.csv}.

\section{Quy trình huấn luyện theo bốn giai đoạn}

Bốn giai đoạn được chạy liên hoàn trong \texttt{run\_pipeline.ipynb} (mỗi phase là một hàm có phạm
vi riêng, dùng chung cấu hình ở đầu file), theo thứ tự phụ thuộc minh hoạ ở Hình~\ref{fig:phase-flow}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  font=\small,
  ph/.style={rectangle, rounded corners, draw, thick, align=center, minimum height=1.0cm, text width=3.0cm},
  arr/.style={-{Stealth[length=2.5mm]}, thick},
]
  \node[ph, fill=blue!8] (p1) {\textbf{Phase 1}\\Train từ đầu\\\scriptsize sinh checkpoint \texttt{cmo}};
  \node[ph, fill=orange!12, below left=1.0cm and -0.5cm of p1] (p0) {\textbf{Phase 0}\\Tái dùng checkpoint};
  \node[ph, fill=green!12, below right=1.0cm and -0.5cm of p1] (p3) {\textbf{Phase 3}\\Nghiên cứu nguồn\\tri thức (cần \texttt{cmo})};
  \node[ph, fill=green!8, right=1.4cm of p1] (p2) {\textbf{Phase 2}\\Thích nghi CLIP\\\scriptsize (chỉ cần dataset)};
  \draw[arr] (p1) -- (p0);
  \draw[arr] (p1) -- (p3);
  \node[above=0.2cm of p2, font=\scriptsize, text=green!40!black] {song song};
\end{tikzpicture}
\caption{Phụ thuộc giữa các giai đoạn. Phase 1 sinh checkpoint \texttt{cmo} làm đối chứng cho Phase
0 và Phase 3; Phase 2 độc lập (chỉ cần dataset) nên chạy song song được.}
\label{fig:phase-flow}
\end{figure}

Mỗi lần chạy ghi lại \texttt{config.json}, lịch sử theo epoch (\texttt{metrics.csv}), checkpoint tốt
nhất (\texttt{best\_model.pt}) cùng các hình (confusion matrix, t-SNE) vào \texttt{outputs/<method>/};
cuối cùng, \texttt{compare\_runs} dựng bảng so sánh được sắp theo balanced accuracy.

\section{Kết quả định lượng trên CIFAR-100-LT}

\subsection{Track 1 --- Leaderboard huấn luyện từ đầu}

Bảng~\ref{tab:cifar-scratch} cho thấy một bức tranh khá rõ theo tầng can thiệp. Baseline đạt $0.401$
balanced accuracy nhưng few-shot chỉ vỏn vẹn $0.079$ --- đuôi gần như chết. Can thiệp ở tầng
\emph{mất mát} (balanced\_softmax) nâng few-shot lên $0.181$ (khoảng 2.3 lần), còn can thiệp đồng
thời ở \emph{dữ liệu} và \emph{mất mát} (cmo) đạt cao nhất với balanced accuracy $0.468$ và few-shot
$0.299$ (gần 4 lần baseline). Một điểm thú vị là cmo và balanced\_softmax chấp nhận \emph{hy sinh
một chút head} (many giảm từ $0.707$ xuống $0.641$) để \emph{cứu lấy đuôi} --- đúng với bản chất
đánh đổi của bài toán long-tail.

\begin{table}[H]
\centering
\caption{CIFAR-100-LT --- Track 1, huấn luyện từ đầu (\texttt{comparison\_train\_from\_scratch.csv}).
Sắp theo balanced accuracy.}
\label{tab:cifar-scratch}
\begin{tabular}{lcccc}
\toprule
\textbf{Phương pháp} & \textbf{bal-acc} & \textbf{many} & \textbf{medium} & \textbf{few} \\
\midrule
\textbf{cmo} & \textbf{0.468} & 0.641 & 0.441 & \textbf{0.299} \\
balanced\_softmax & 0.441 & 0.681 & 0.424 & 0.181 \\
decoupling & 0.407 & 0.706 & 0.375 & 0.094 \\
baseline & 0.401 & 0.707 & 0.371 & 0.079 \\
supcon & 0.367 & 0.668 & 0.361 & 0.021 \\
meta & 0.351 & 0.558 & 0.390 & 0.064 \\
\bottomrule
\end{tabular}
\end{table}

Như vậy trần của việc học-từ-đầu rơi vào khoảng $0.47$ balanced accuracy (tail $\approx 0.30$).
SupCon kém kỳ vọng ở đuôi (few $0.021$) bởi với 5 ảnh/lớp, số cặp dương quá ít để hình thành cụm
tương phản tốt cho tail; còn Meta (ProtoNet) yếu ở $100$-way như đã dự đoán, vì vốn được thiết kế
cho few-way.

\subsection{Track 2 --- Tái dùng checkpoint}

Bảng~\ref{tab:cifar-phase0} trình bày hiệu quả của các kỹ thuật reuse. CLIP fusion
(\texttt{vlm\_fusion}) dẫn đầu ($0.681$) và vượt cả hai chuyên gia thành phần, cho thấy giá trị của
tri thức ngôn ngữ ngay cả khi mới chỉ ``trộn''. Các kỹ thuật thuần-thị-giác (ensemble, tier\_fusion,
$\tau$-norm) chỉ nâng nhẹ so với mô hình đơn và chủ yếu ở head; trong nhóm này, $\tau$-norm cải
thiện đuôi rõ nhất ($0.364$).

\begin{table}[H]
\centering
\caption{CIFAR-100-LT --- Track 2, tái dùng checkpoint (\texttt{comparison\_phase0.csv}).}
\label{tab:cifar-phase0}
\begin{tabular}{lcccc}
\toprule
\textbf{Kỹ thuật} & \textbf{bal-acc} & \textbf{many} & \textbf{medium} & \textbf{few} \\
\midrule
\textbf{vlm\_fusion} (CLIP $+$ vision) & \textbf{0.681} & 0.810 & 0.645 & 0.571 \\
clip\_only (zero-shot) & 0.627 & 0.638 & 0.605 & 0.638 \\
tau\_norm & 0.543 & 0.720 & 0.521 & 0.364 \\
ensemble\_tta & 0.494 & 0.772 & 0.493 & 0.172 \\
tier\_fusion & 0.489 & 0.775 & 0.513 & 0.126 \\
\bottomrule
\end{tabular}
\end{table}

Có một điểm về tính nhất quán cần nói rõ ở đây. Bộ checkpoint dùng cho Phase 0 cho số mô hình-đơn
\emph{cao hơn} leaderboard from-scratch ở Bảng~\ref{tab:cifar-scratch} (ví dụ \texttt{cmo} đạt
$0.514$ ở Phase 0 so với $0.468$ ở Track 1), nhiều khả năng do bộ này dùng \texttt{pretrained=True}.
Vì vậy Bảng~\ref{tab:cifar-phase0} nên được đọc như phần \emph{minh hoạ các kỹ thuật reuse} chứ
không trộn trực tiếp con số với Track 1 hay Track 3; đối chứng from-scratch chuẩn vẫn là Track 1 và
Track 3.

\subsection{Track 3 --- Thích nghi mô hình nền tảng (kết quả chính)}

Đây là bảng quan trọng nhất của báo cáo (Bảng~\ref{tab:cifar-foundation}). Track tri thức ngoài tạo
ra một bước nhảy quyết định: từ trần from-scratch $0.47$ lên $\mathbf{0.82}$ balanced accuracy, với
few-shot tăng từ $0.30$ lên $\mathbf{0.755}$. Mô hình nền tảng thị giác thứ hai
(\texttt{dino\_lift}, $0.811$) vượt rõ LIFT-CLIP ($0.717$), còn fusion gộp các expert đạt cao nhất
($0.823$) mà \emph{không tụt ở bất kỳ nhóm-shot nào}.

\begin{table}[H]
\centering
\caption{CIFAR-100-LT --- Track 3, thích nghi mô hình nền tảng (\texttt{comparison\_phase3.csv} \&
\texttt{comparison\_vlm\_phase2.csv}). Sắp theo balanced accuracy.}
\label{tab:cifar-foundation}
\begin{tabular}{llcccc}
\toprule
\textbf{Expert} & \textbf{Nguồn tri thức} & \textbf{bal-acc} & \textbf{many} & \textbf{medium} & \textbf{few} \\
\midrule
\textbf{fusion\_greedy} & gộp & \textbf{0.823} & 0.890 & 0.813 & \textbf{0.755} \\
fusion\_tailaware & gộp & 0.820 & 0.893 & 0.803 & 0.753 \\
\textbf{dino\_lift} & thị giác thứ hai & \textbf{0.811} & 0.872 & 0.813 & 0.737 \\
lift (CLIP) & vision-language & 0.718 & 0.782 & 0.727 & 0.635 \\
tip\_adapter & truy hồi & 0.668 & 0.679 & 0.648 & 0.678 \\
tip\_adapter\_f & truy hồi (FT) & 0.643 & 0.821 & 0.672 & 0.400 \\
clip\_llm & ngôn ngữ & 0.626 & 0.618 & 0.606 & 0.658 \\
clip\_zeroshot & --- & 0.627 & 0.638 & 0.605 & 0.638 \\
\texttt{cmo} (đối chứng) & nội tại & 0.462 & 0.614 & 0.443 & 0.307 \\
\bottomrule
\end{tabular}
\end{table}

Hình~\ref{fig:fewshot-progression} trực quan hoá thông điệp cốt lõi: few-shot accuracy \emph{tăng
đơn điệu} qua bốn mốc đại diện cho mạch tiếp cận.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar, width=0.85\textwidth, height=6cm,
  bar width=20pt,
  ymin=0, ymax=0.85,
  ylabel={Few-shot accuracy (đuôi)},
  symbolic x coords={baseline,cmo,CLIP-zeroshot,LIFT-CLIP,DINOv2-LIFT,fusion},
  xtick=data, x tick label style={rotate=20, anchor=east, font=\small},
  nodes near coords, nodes near coords style={font=\scriptsize},
  enlarge x limits=0.12,
]
\addplot[fill=blue!40] coordinates {
  (baseline,0.079) (cmo,0.299) (CLIP-zeroshot,0.638) (LIFT-CLIP,0.635) (DINOv2-LIFT,0.737) (fusion,0.755)
};
\end{axis}
\end{tikzpicture}
\caption{CIFAR-100-LT: few-shot accuracy tăng đơn điệu từ baseline ($0.079$) $\to$ cmo (trần
from-scratch, $0.299$) $\to$ track tri thức ngoài (CLIP $0.638$, LIFT, DINOv2 $0.737$, fusion
$0.755$). Đuôi được cứu chủ yếu nhờ \emph{mượn tri thức ngoài}.}
\label{fig:fewshot-progression}
\end{figure}

\section{Kết quả định lượng trên CUB-200-LT (fine-grained)}

Trên miền fine-grained khó hơn, khoảng cách giữa hai track còn nới rộng thêm
(Bảng~\ref{tab:cub-foundation}). Huấn luyện-từ-đầu gần như vô dụng, với balanced accuracy chỉ quanh
mức $0.20$--$0.27$ (cmo $0.195$, baseline $0.258$), trong khi track nền tảng đạt $\mathbf{0.85}$ ---
một bằng chứng mạnh cho thấy \emph{tri thức ngoài là thiết yếu} khi dữ liệu vừa ít vừa khó. Một lần
nữa \texttt{dino\_lift} ($0.841$) bỏ xa LIFT-CLIP ($0.670$), thậm chí còn mạnh hơn so với trên
CIFAR; điều này phù hợp với việc DINOv2 (patch nhỏ $14$, tự giám sát) bắt chi tiết fine-grained rất
tốt.

\begin{table}[H]
\centering
\caption{CUB-200-LT --- Track 3, thích nghi mô hình nền tảng (\texttt{comparison.csv}).}
\label{tab:cub-foundation}
\begin{tabular}{llcccc}
\toprule
\textbf{Expert} & \textbf{Nguồn tri thức} & \textbf{bal-acc} & \textbf{many} & \textbf{medium} & \textbf{few} \\
\midrule
\textbf{fusion\_greedy} & gộp & \textbf{0.849} & 0.858 & 0.824 & \textbf{0.864} \\
fusion\_tailaware & gộp & 0.846 & 0.859 & 0.824 & 0.854 \\
\textbf{dino\_lift} & thị giác thứ hai & \textbf{0.841} & 0.851 & 0.812 & 0.859 \\
lift (CLIP) & vision-language & 0.670 & 0.741 & 0.650 & 0.593 \\
tip\_adapter\_f & truy hồi (FT) & 0.649 & 0.758 & 0.623 & 0.529 \\
tip\_adapter & truy hồi & 0.565 & 0.637 & 0.521 & 0.516 \\
clip\_llm & ngôn ngữ & 0.484 & 0.518 & 0.458 & 0.466 \\
clip\_zeroshot & --- & 0.460 & 0.494 & 0.433 & 0.445 \\
\texttt{cmo} (đối chứng) & nội tại & 0.195 & 0.256 & 0.186 & 0.120 \\
\bottomrule
\end{tabular}
\end{table}

\section{Nghiên cứu loại trừ (Ablation Study)}

\subsection{Ablation A --- đóng góp của pipeline so với backbone (DINOv2)}

Câu hỏi đặt ra là: trong kết quả cuối cùng, bao nhiêu phần đến từ \emph{backbone mạnh} (DINOv2) và
bao nhiêu đến từ \emph{pipeline của nhóm} (loss nhận-biết-đuôi $+$ adapter/cosine head)? Để tách
bạch, nhóm dựng một chuỗi ba mốc trên \emph{cùng} đặc trưng DINOv2
(Bảng~\ref{tab:ablationA}). Trên CIFAR, chỉ riêng việc đổi cross-entropy sang Balanced Softmax đã
kéo few-shot từ $0.020$ lên $0.325$ (đóng góp của \emph{loss}); thêm adapter và cosine init đẩy tiếp
lên $0.737$ (đóng góp của \emph{kiến trúc}). Trên CUB hiệu ứng còn rõ rệt hơn
($0.077\to0.675\to0.859$). Kết luận rút ra là \emph{backbone mạnh thôi chưa đủ}: nếu chỉ gắn một đầu
tuyến tính ngây thơ thì đuôi vẫn sụp, và phần lớn giá trị thực sự đến từ pipeline tail-aware của
nhóm.

\begin{table}[H]
\centering
\caption{Ablation A trên đặc trưng DINOv2: tách đóng góp của loss và kiến trúc.}
\label{tab:ablationA}
\begin{tabular}{lcccc}
\toprule
& \multicolumn{2}{c}{\textbf{CIFAR-100-LT}} & \multicolumn{2}{c}{\textbf{CUB-200-LT}} \\
\textbf{Mốc} & bal-acc & few & bal-acc & few \\
\midrule
dino\_linear\_probe (Linear $+$ CE) & 0.463 & 0.020 & 0.490 & 0.077 \\
dino\_linear\_probe\_bs (Linear $+$ Balanced-Softmax) & 0.657 & 0.325 & 0.744 & 0.675 \\
\textbf{dino\_lift} ($+$adapter $+$cosine init) & \textbf{0.811} & \textbf{0.737} & \textbf{0.841} & \textbf{0.859} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablation B --- độ sâu fine-tune CLIP: fine-tune nặng làm hại đuôi}

Đây là ablation nhằm biện minh cho thiết kế \emph{đóng băng}. Nhóm fine-tune CLIP ở ba độ sâu tăng
dần --- chỉ head, rồi block cuối, rồi toàn bộ visual encoder --- với Balanced Softmax
(Bảng~\ref{tab:ablationB}, trên CIFAR). Balanced accuracy có tăng theo độ sâu
($0.461\to0.536\to0.558$), nhưng few-shot lại tệ đi và luôn ở mức rất thấp (cao nhất chỉ $0.128$).
So với LIFT \emph{đóng băng} (few-shot $0.635$), mọi cấu hình fine-tune đều thua xa ở đuôi. Kết quả
này xác nhận đúng luận điểm của LIFT: với 5 ảnh/lớp, việc lan truyền gradient vào backbone gây
overfit và xói mòn tri thức zero-shot ở các lớp hiếm.

\begin{table}[H]
\centering
\caption{Ablation B trên CIFAR-100-LT: độ sâu fine-tune CLIP (Balanced Softmax). G-mean rất nhỏ cho
thấy nhiều lớp tail bị ``chết''.}
\label{tab:ablationB}
\begin{tabular}{lcccc}
\toprule
\textbf{Cấu hình} & \textbf{bal-acc} & \textbf{many} & \textbf{few} & \textbf{G-mean} \\
\midrule
ft\_linear\_probe & 0.461 & 0.785 & 0.086 & 0.004 \\
ft\_last\_block & 0.536 & 0.850 & 0.128 & 0.005 \\
ft\_full\_ft & 0.558 & 0.874 & 0.111 & 0.007 \\
\midrule
\textbf{lift (đóng băng)} & \textbf{0.718} & 0.782 & \textbf{0.635} & \textbf{0.697} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Augment đặc trưng: sinh dữ liệu và mixup không giúp}

Khi so sánh ba biến thể trên cùng nền LIFT-CLIP (Bảng~\ref{tab:augment}), cả \emph{sinh đặc trưng
bằng diffusion} (\texttt{lift\_clip\_diff}) lẫn \emph{feature mixup} (\texttt{lift\_clip\_mixup}) đều
\emph{thấp hơn} LIFT-CLIP gốc trên \emph{cả hai} dataset. Đây là một kết quả âm trung thực: khi đặc
trưng nền tảng đã đủ mạnh, việc augment thêm chủ yếu đưa vào nhiễu thay vì thông tin mới. Riêng
diffusion còn làm tail tệ đi rõ rệt trên CIFAR (few $0.505$).

\begin{table}[H]
\centering
\caption{Augment đặc trưng so với LIFT-CLIP gốc (balanced accuracy).}
\label{tab:augment}
\begin{tabular}{lcc}
\toprule
\textbf{Phương pháp} & \textbf{CIFAR-100-LT} & \textbf{CUB-200-LT} \\
\midrule
lift\_clip (gốc) & \textbf{0.717} & \textbf{0.670} \\
lift\_clip\_mixup (feature mixup) & 0.698 & 0.654 \\
lift\_clip\_diff (diffusion) & 0.675 & 0.655 \\
\bottomrule
\end{tabular}
\end{table}

\section{Kết quả định tính}

Một ``slide demo đuôi'' điển hình minh hoạ rất rõ giá trị của tri thức ngoài: với một ảnh thuộc lớp
hiếm, mô hình thị giác from-scratch thường dự đoán sai (gán nhầm sang một lớp head trông tương tự),
trong khi LIFT/DINOv2 --- nhờ khởi tạo ngữ nghĩa và loss nhận-biết-đuôi --- lại dự đoán đúng. Các
trường hợp như vậy cho thấy bằng trực giác vì sao tri thức ngoài cứu được đuôi. Những hình tương ứng
(confusion matrix, t-SNE của đặc trưng) được sinh ra từ pipeline đánh giá và có thể chèn vào các vị
trí dưới đây.

\begin{figure}[H]
\centering
\fbox{\parbox[c][3.2cm][c]{0.8\textwidth}{\centering \texttt{[FIGURE: qualitative\_results]}\\[2pt]
\small Ví dụ dự đoán trên lớp tail: vision (sai) vs CLIP/LIFT/DINOv2 (đúng).}}
\caption{Phân tích định tính trên các lớp đuôi (placeholder --- chèn ảnh từ pipeline đánh giá).}
\label{fig:qualitative}
\end{figure}

\begin{figure}[H]
\centering
\fbox{\parbox[c][3.2cm][c]{0.8\textwidth}{\centering \texttt{[FIGURE: confusion\_matrix\_comparison]}\\[2pt]
\small Confusion matrix chuẩn hoá: baseline (tail ``chết'') vs dino\_lift (tail sống).}}
\caption{So sánh confusion matrix: phương pháp từ đầu bỏ rơi đuôi, track nền tảng phân bố đều hơn.}
\label{fig:confmat}
\end{figure}

\section{Thảo luận}

\subsection{Các phát hiện chính (nhất quán trên cả hai dataset)}
\begin{enumerate}
  \item \textbf{Tri thức ngoài là bước nhảy quyết định.} Trên CIFAR, kết quả đi từ from-scratch
        $0.47$ lên nền tảng $0.82$; trên CUB là từ $0.20$--$0.27$ lên $0.85$. Chênh lệch càng lớn ở
        miền khó (fine-grained CUB), nơi học-từ-đầu gần như thất bại.
  \item \textbf{Nguồn cứu đuôi nhất là mô hình nền tảng thị giác thứ hai (DINOv2).} \texttt{dino\_lift}
        vượt LIFT-CLIP ở cả hai dataset ($0.811$ vs $0.718$; $0.841$ vs $0.670$) và là trụ cột của
        fusion. DINOv2 được huấn luyện tự giám sát với patch $14$, nên bắt chi tiết fine-grained tốt
        hơn CLIP ViT-B/32 ở ảnh nhỏ.
  \item \textbf{Ngôn ngữ (LLM) giúp ít.} \texttt{clip\_llm} gần như ngang \texttt{clip\_zeroshot}
        (CIFAR $0.626$ vs $0.627$; CUB $0.484$ vs $0.460$, chỉ nhích nhẹ). Mô tả giàu không bù được
        hạn chế của CLIP ViT-B/32 ở ảnh nhỏ và dữ liệu fine-grained.
  \item \textbf{Sinh dữ liệu và mixup không giúp} (cả hai dataset đều thấp hơn LIFT-CLIP gốc): khi
        đặc trưng đã mạnh, augment thêm chỉ đưa vào nhiễu.
  \item \textbf{Đóng góp của pipeline đo được rõ ràng} (Ablation A): chỉ riêng Balanced Softmax đã
        cứu đuôi từ $\sim0.02$ lên $0.33$ (CIFAR) và từ $0.08$ lên $0.68$ (CUB); thêm adapter cùng
        cosine init đẩy tiếp lên $0.74$/$0.86$.
  \item \textbf{Fine-tune nặng làm hại đuôi} (Ablation B): full-FT cho few-shot $0.111$ so với LIFT
        đóng băng $0.635$ --- một biện minh trực tiếp cho thiết kế đóng băng.
  \item \textbf{Bù trừ có nhưng ở mức nhẹ và an toàn.} \texttt{fusion\_greedy} vượt expert đơn tốt
        nhất ở cả hai dataset ($0.823>0.811$; $0.849>0.841$) mà không tụt ở nhóm nào, vì greedy bảo
        đảm $\ge$ best-single trên validation cho từng nhóm.
\end{enumerate}

\subsection{Điểm mạnh và hạn chế}
Về điểm mạnh, nghiên cứu có một khung thống nhất, đa-dataset, với giao thức chọn-mô-hình-trên-val
nghiêm ngặt; các kết luận nhất quán trên hai miền và hai quy mô lớp; có ablation tách bạch đóng góp
và trung thực với cả các kết quả âm.

Để bảo đảm minh bạch, nhóm cũng nêu rõ hai ghi chú thực nghiệm (caveat) liên quan đến \emph{breakdown},
vốn không ảnh hưởng tới balanced accuracy hay các kết luận. Thứ nhất, \emph{CIFAR Phase 0} chạy trên
một bộ checkpoint cho số mô hình-đơn cao hơn leaderboard from-scratch (có thể do
\texttt{pretrained=True}), nên Bảng~\ref{tab:cifar-phase0} chỉ nên xem là minh hoạ kỹ thuật reuse,
không so trực tiếp con số với Track 1/3. Thứ hai, \emph{CUB from-scratch} được chấm khi ngưỡng
nhóm-shot còn ở mặc định ($100/20$), nên nhóm \emph{many} rỗng và cột many/medium/few của riêng nhóm
này \emph{không hợp lệ}; tuy vậy balanced accuracy không phụ thuộc cách chia nhóm nên vẫn đúng, và
track nền tảng (Bảng~\ref{tab:cub-foundation}) đã dùng đúng ngưỡng $20/10$. Cả hai điểm này chỉ liên
quan đến breakdown; balanced accuracy và mọi kết luận đều được giữ nguyên.
