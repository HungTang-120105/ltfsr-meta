# Only packages actually imported by the project.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.0
pandas>=1.5.0
Pillow>=9.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Optional — only for the CLIP vision-language fusion (notebooks/phase0_reuse.ipynb):
#   pip install open_clip_torch
